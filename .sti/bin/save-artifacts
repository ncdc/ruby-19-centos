#!/bin/sh -e

echo "---> Saving build-artifacts"

pushd ${HOME} >/dev/null

if [ -d ./bundle/ruby -a -f ./src/${APP_ROOT}/Gemfile.lock ]; then
  tar zcf ${STI_ARTIFACTS_DIR}/gems.tgz bundle/ruby src/${APP_ROOT}/Gemfile.lock
fi

popd >/dev/null
